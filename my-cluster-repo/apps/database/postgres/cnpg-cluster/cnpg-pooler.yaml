apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: n8n-db-pooler
  namespace: default
spec:
  cluster:
    name: n8n-db-cluster
  instances: 2
  monitoring:
    enablePodMonitor: true
  type: rw
  pgbouncer:
    poolMode: transaction